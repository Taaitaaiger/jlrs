[package]
name = "ffi-validator"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
syn = { version = "3", features = ["full", "extra-traits", "printing"] }

